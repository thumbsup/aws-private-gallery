{
  "name": "lambda-cloudfront-cookies",
  "version": "1.0.0",
  "description": "AWS Lambda function that creates CloudFront cookies for valid users",
  "author": "thumbsup",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "test": "standard && require-lint --ignore-missing aws-sdk"
  },
  "dependencies": {
    "aws-cloudfront-sign": "^2.2.0",
    "axios": "^0.18.0",
    "jsonwebtoken": "^8.2.0",
    "jwk-to-pem": "^2.0.0"
  },
  "devDependencies": {
    "require-lint": "^1.3.0",
    "standard": "^8.6.0"
  }
}
