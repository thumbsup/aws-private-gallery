{
  "name": "lambda-cloudfront-cookies",
  "version": "1.0.0",
  "description": "AWS Lambda function that creates CloudFront cookies for valid users",
  "author": "thumbsup",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "test": "standard && require-lint --ignore-missing @aws-sdk/client-ssm"
  },
  "dependencies": {
    "aws-cloudfront-sign": "^3.0.2",
    "axios": "^1.6.8",
    "jsonwebtoken": "^9.0.2",
    "jwk-to-pem": "^2.0.5"
  },
  "devDependencies": {
    "require-lint": "^2.0.3",
    "standard": "^17.1.0"
  }
}
